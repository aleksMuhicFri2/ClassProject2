<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.ClassProject2" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Dark background color -->
        <item name="android:background">@color/very_dark_blue</item>

        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>

        <!-- Primary, secondary colors -->
        <item name="colorPrimary">@color/very_dark_purple</item>
        <item name="colorSecondary">@color/very_dark_blue</item>

    </style>
</resources>
